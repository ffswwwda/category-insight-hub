# 2026-07-17 工作日志

## anime.js 集成到 category-insight-hub.html
- 克隆 juliangarnier/anime（v4.5.0），vendored UMD 构建到 `vendor/anime.umd.min.js`（116K，离线/ GitHub Pages 安全）。
- **关键坑**：anime.js v4 全局 `anime` 是「命名空间对象」，**不是函数**。正确调用 `anime.animate(targets, params)` 两参数；旧版 `anime({targets:...})` 会报 "anime is not a function"。已全量修正 5 处。
- VOC 标签体系三段布局改造：
  - 中间 16px 列加宽到 78px，把 `→` 箭头换成 **SVG 三次贝塞尔曲线**（`vector-effect:non-scaling-stroke` 保证拉伸后线宽恒定），用 `strokeDashoffset` 逐段「描绘」动画。
  - 三区用底色 + 虚线分隔切分更明显（user/insight/actions）。
  - 行 + 动作卡 stagger 入场；模块展开改用 anime 高度动画。
  - 每个动画入口加 `if(!window.anime) return;` 优雅降级。
- 同步建 WorkBuddy 技能包 `~/.workbuddy/skills/animejs-animations/SKILL.md`（沉淀 v4 API 差异 + 曲线连接器/stagger/时间线/高度动画模板）。
- 已 jsdom 验证（3 行×2 曲线、15 动作卡、toggle/切换正常、无报错），git 提交并推送。
