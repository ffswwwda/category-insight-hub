# 2026-07-16 工作记录

## 修复 category-insight-hub.html 白屏（重大事故）

### 根因
- 昨日会话中，为接入 248 条真实德国数据（de_records.json），用 Python 做"删除残留内联数据"编辑时，**把 `DATA` 对象在 `sourceRecords` 之后、`purposeDict` 之前的全部顶层字段整段删掉**（workspaces / purposes / results / framework / segmentDims / segmentRecords / skills / categoryInsights / tagging 等共 9 个），且无 git/备份可恢复。
- 页面靠外部 `<script src="de_records.js">` 提供数据；该外部文件加载失败时主脚本 `ReferenceError: DE_RECORDS_FULL is not defined`，整页白屏。
- `init()` 第一行 `DATA.workspaces[state.workspace].desc` 因 `DATA.workspaces` 不存在直接抛 `Cannot read properties of undefined (reading 'A')`（state.workspace 默认 "A"），UI 零渲染。

### 处置
- **彻底重写** `category-insight-hub.html` 为聚焦单文件看板（用户原话：只把德国数据源用真实数据搭看板，别改架构）。
- 248 条数据 **内联** 进 HTML（不再依赖外部 js 文件，双击/预览均可开）；JSON 中 `</` 转义防 script 提前闭合。
- 功能：自然语言筛选（价格/星级/FBA/自发/中国·本土卖家/视频/A+/销量，大小写不敏感，"视频或A+"OR 语义，含"理解栏"回显）+ 11 个快捷 chip + 排序 + 6 KPI + 子类目/卖家来源/价格带/星级四维分布 + 低分洼地(≤3星) + 可展开原始表。
- 验证：jsdom 真实运行 —— 无运行时错误；248 全量渲染；NL "20欧以下 FBA 中国卖家 有视频"→20 款；chip "1-3星差评"→24 款。全部通过。

### 经验教训（重要，下不为例）
- 对单文件 HTML 做"删除某段数据"类脚本编辑极危险：正则误伤相邻结构会静默破坏对象。此类操作必须先备份整文件（cp 一份 .bak），验证后再删。
- 外部 `<script src>` 数据在 file:// 或预览下易加载失败致白屏；**真实数据一律内联**进单文件最稳。
- 用户明确偏好：干净亮色、克制、无 emoji；只做被要求的事，勿顺手改架构/设计。

## 重建主站外壳（用户纠正"只留看板"）

### 用户反馈
- 用户怒：白屏修复时把整页重写成"只剩德国看板"，丢失了原主页面（含"数据源"板块，里面列德国/其他数据源，点进去才到看板）。原话："你只留这个界面？？？"
- 确认方向：重建主站外壳，把德国看板嵌进"数据源"板块。

### 处置（第二次重建——按用户截图结构）
- 用户再次纠正：原页面不是扁平4个nav（概览/数据源/分析/标签体系），而是**两个工作区方向**（用户洞察·拓展 / 运营目的·拓展）作为左侧核心导航，每个工作区内再有「数据源 / 分析框架 / AI看板 / 标签体系」子板块tab。顶栏有搜索+筛选+导出按钮。
- 最终 `category-insight-hub.html` 重建为：左侧2个工作区按钮；顶栏标题+搜索/筛选/导出（和截图布局一致）；每个工作区下4个子板块tab；德国在「用户洞察→数据源」中，点进去iframe加载 de-dashboard.html。
- `de-dashboard.html` 不变（独立看板，248条内联）。
- 验证 jsdom：主站无报错、5 卡片(1 已接入)、点德国→view-de 激活且 iframe src=de-dashboard.html、导航正常；看板无报错、248 渲染、11 chip、goBack 存在。全部通过。
- 遗留：`de_records.js`(161KB) 已不被引用，可删可留（看板已内联数据）。

## 第三次重构——按三张完整架构截图 + Radium 未来感玻璃拟态风格

### 用户需求（贴3张架构截图 + 大段设计说明）
- 完整 IA：数据源池(s1-s11 共11源) + 双工作区(A用户洞察·创意 / B运营目的·解题)×共8个目的工作区；每个目的绑定不同数据源+方法+工具；导出到智能体(提示词构建器)。
- 目的工作区双模式：结构化(默认，系统给答案) vs 无限画布(拖拽/连线/缩放/localStorage持久化)。
- 三层级落地：数据源看板(二级页) / 目的工作区(智能建议+需求四象限+细分市场+购买旅程) / 全量上下文提示词(复述理解+区分已支持/需验证/外部补充+引用标出处)。
- 明确要求用 radium-design-style 技能风格(玻璃拟态、未来感、浅色暖灰白底)重构。

### 处置
- 加载 radium-design-style 技能，用 cat heredoc 完全重写 `category-insight-hub.html`(~59KB 单文件)。
- 结构：左侧 sidebar 双工作区 tab(insight/ops) + 主区(topbar+subnav+board)；board 3 view：viewGrid(目的卡片网格+数据源池bar) / viewWorkspace(structPanel + canvasArea 双模式) / viewDe(德国看板 iframe)。
- DATA：sources(s1-s11，s1/s8 ready) + workspaces(insight 4目的 + ops 4目的) + seedInsights(8目的示例)。
- 引擎：无限画布(SVG贝塞尔连线/滚轮缩放/拖拽/localStorage键cih_canvas_<purposeId>) + VOC规则引擎(7规则含negPatterns否定保护) + 提示词构建器(gatherContext→generatePrompt)。
- CSS：Radium 浅色玻璃拟态 --bg:#F5F3F1 --accent:#0891B2 --glass-bg:rgba(255,255,255,.55) blur(20px)。

### 修复的语法错误(heredoc 引入)
- `Math.max(.2)Math.min`→`Math.max(.2,Math.min(5,...))`(2处)；`ctx purpose=`→`ctx.purpose=`；`icon:' '&#x1F4A1;'`→`icon:'&#x1F4A1;'`；`body':'`→`body:'`；`。'\+`→`。'+`(转义反斜杠)。
- 验证 jsdom 通过：无运行时错误、11源、2工作区、8目的卡片、数据源池25节点、sidebar tab、德国 iframe 入口正常。已清理探针。

## 部署到 GitHub Pages

### 操作
- 仓库 https://github.com/ffswwwda/category-insight-hub （空仓，默认分支 main）。
- 本地 git init + commit（category-insight-hub.html 59KB + de-dashboard.html 200KB），用 token 推送到 origin main。
- 通过 API 启用 Pages：source={branch:main,path:/}。
- iframe 引用为相对路径 `src="de-dashboard.html"`，Pages 同根目录可正常加载。

### 验证
- 首次构建约 45s 完成；线上 HTTP 200、size 与原文件一致（index 59896 / de 204540）。
- 线上地址：**https://ffswwwda.github.io/category-insight-hub/category-insight-hub.html**（德国看板在其 iframe 内）。
- 本地 .git 保留（便于后续 `git push` 更新），remote 已含 token 凭证（提醒用户：token 已写入本地 remote URL，如需安全可改用 gh auth）。

## 重构左侧导航（用户纠正"完全不像"）

### 用户澄清的真实逻辑
- 左侧**三个大板块**：①数据源板块（共享不变）②分析框架板块（共享不变）③动态板块（随工作区切换）。
- 切换两个工作区（用户洞察/运营目的）后，**只有第三板块内容变化**：
  - 用户洞察侧：市场细分/用户问题研究/品类机会发现/新品概念孵化
  - 运营目的侧：产品优化/营销建议/新品规划/新品打造
- 我之前做成了「工作区tab + 顶部固定4子板块tab」，结构错误。

### 处置
- 重写左侧 .sidebar：工作区切换(.ws-tabs) + .nav-tree 三 section（navSources / navFramework / navPurpose）。
- 新增 DATA.frameworks（5个共享框架：需求四象限/JTBD/购买旅程/动机编码/VOC标签）。
- 新增 JS：renderNavShared / renderNavPurpose / setActiveNav / openSource / openFramework；switchWorkspace 现在调用 renderNavPurpose 重渲染第三板块。
- 新增 viewSource / viewFramework 两个视图；修 openSource/openDeDashboard 中 `$('viewDe')` 漏写 `#` 的 bug（导致 DE 看板打不开）。
- 风格沿用 Radium 浅色玻璃拟态（--bg 暖灰白、.nav-sec glass、.nav-item active 左侧强调色条）。
- 验证 jsdom：3 section / 11源 / 5框架 / 4目的；切换 ops→tag"运营"、4 ops 目的；开 s8→viewDe；开 quad→viewFramework；进 purpose→viewWorkspace 且 nav 高亮。全部通过。
- 已 commit + push（2ef7672）。线上已含新 nav（curl 校验通过）。

## 三层级目的架构落地（用户最新详细设计说明）

### 用户定义的三个层级（基于目的）
- **L1 数据源看板（二级页）**：8+个数据源，每个点进去是单独二级页，支持筛选该源、导出、生成看板报告、内置数据源综合报告。面向只想看某源或拉部分源做其他工作的人。
- **L2 目的工作区（skill简化分析）**：固定目的→进工作区，需精准数据源+交叉。支持自然语言获取特定数据；用户不知该取哪些源→给建议（交叉分析数据源+用哪些框架）；展示基础分析结果（需求四象限/细分市场/购买旅程）。
- **L3 提示词生成（结合L2深度多轮）**：有目的+数据源+上下文时，复制给智能体；提示词必须精准，把智能体当第一次看数据。

### 处置
- **L1**：重写 openSource → 非 dashboard 源走新 renderSourceExplorer（genSampleRows 确定性样本、applySourceFilter 筛选、exportSourceData 导出 CSV Blob、genSourceReport 综合报告含维度分布图）。s8 仍走 de-dashboard.html iframe（其专属完整看板）。
- **L2**：renderStructPanel 新增三段——①自然语言获取(runNLAcquire+matchSourcesByNL 关键词映射)②智能建议(renderSuggestions: suggestCrossSources 跨源/同市场评分 + suggestFrameworks 按 PURPOSE_FW 映射, 默认预选)③基础分析(renderQuadrant 2x2 SVG / renderSegments / renderJourney 5阶段)。
- **L3**：gatherContext 纳入 nlAcquired 源、selectedFrameworks、analysisSummary；generatePrompt 重写——强化"第一次接触数据"角色设定 + 一二三…八段结构（目的/数据源/框架/已有结论/洞察/画布）+ 输出要求（复述理解/三类结论标注+标出处/行动建议/多轮深化）；修原 `question||` 运算符优先级 bug。
- 修复 toast: `$('toast')` 在 jsdom 内 id 解析为 null 的怪异（querySelector('#toast') 偶发），改用 document.getElementById + 空守卫，更稳。
- 新增 CSS：.src-explorer/.se-* 筛选表报、.nl-*/.sg-* 建议、.analysis-grid/.quad-svg/.journey-flow 等（Radium 浅色玻璃拟态一致）。
- 验证 jsdom 22 项全 PASS、零运行时错误；commit + push（8d1043a）；线上 HTTP 200 / 95KB，含全部三层级标记。
- 注：L1 样本为合成数据（genSampleRows 确定性随机），接入真实源后替换 DATA.sources 即可实时刷新。

## Radium 视觉皮肤重做（用户指出"完全不像该技能"）
- 根因：之前只是"浅色玻璃壳"，缺 Radium 签名元素。对照 localhost:9292（AI运营看板，用 css/radium-style.css）与技能 SKILL.md 完整规范。
- 改动（类名全部保留，仅换皮，JS 功能不动）：
  - 背景：径向渐变光斑（青/紫/金低透明）+ 60px 网格（body::before/::after，亮/暗两版）。
  - 主色：扁平 #0891B2 → 霓虹渐变 `linear-gradient(135deg,#00d4ff,#a855f7,#ff6b9d)`，用于品牌名/标题/主按钮/激活态/KPI数字/进度条。
  - 签名交互：自定义发光光标 + 粒子拖尾、滚动进度条（绑定 board/explorer/struct-panel）、按钮涟漪、卡片悬浮上浮+rotateX+顶部渐变条、入场 fadeInUp。
  - 主题切换：顶栏 🌓 按钮，默认 **light**（用户偏好干净亮色 + 技能 light 模式），可切 dark（对标 localhost:9292 霓虹暗色）；localStorage 记忆，已加 try/catch 保护。
- 修复真实 bug：画布平移 `paning`/`panaging` 拼写错误 → `panning`（之前鼠标移动会抛 ReferenceError）。
- 验证 jsdom 15 项全 PASS、零运行时错误；commit + push（0637371）；线上 HTTP 200 / 108KB，含 custom-cursor/rad-grad/scroll-progress/theme-toggle。

## 左侧导航重构为「源数据/逻辑」三大板块 + 动态目的（用户纠正交互逻辑）
- 用户要求：数据源/分析框架 点击后**右侧出现具体内容**（画廊），再点单独项→其看板页；并新增 **AI简化工具**（VOC打标签 + 用户语料分析）。最终结构 = 两大类：
  1. **源数据/逻辑**（共享、不变）：含 3 个可点击导航项 → 数据源画廊 / 分析框架画廊 / AI简化工具
  2. **目的工作区**（动态，随 用户洞察↔运营目的 切换重渲染）
- 改动：sidebar `nav-tree` 改为两类 section（#navShared 由 renderNavShared 填 3 项带数量；#navPurpose 由 renderNavPurpose 填动态目的）。
- 新增视图：viewSourceGallery（多源卡片→openSource 进看板/Explorer）、viewFrameworkGallery、viewAITools（2 工具卡）、viewVocTool（独立 VOC 工具，复用 runVocTagger 并改造成支持指定元素 id）、viewCorpusTool（批量语料分析：逐行跑 VOC 规则→维度频率/负面占比/TOP5）。
- 修复**真 bug**：VOC_RULES 原用 `\b` 词边界，对中文全部失效（规则永不命中）。去掉 `\b` 并重写 7 条规则（覆盖 声音太大 等），另补 corpus 负面情感正则。
## 残留 Unicode 符号清除（12:20 用户反馈）
用户指出截图中 Radium 渐变圆角方块图标风格（紫粉霓虹+圆角胶囊+内嵌标识）符合页面设计，其他散落符号（emoji/unicode箭头等）感觉奇怪。
清理 category-insight-hub.html 中全部 11 处残留 unicode 实体：
- `&#8592;`(←) 返回按钮 5 处 → 纯文字「返回」/「返回数据源」/「返回分析框架」/「返回工具箱」
- `&#8594;`(→) 进入/查看/打开 4 处 → 去掉箭头只保留纯文字
- `&#x25B2;`(▲) 素材折叠 1 处 → 「收起」
- `&#x21B3;`(↳) 方法链路步骤 1 处 → 「↓」
验证：jsdom 全局 &#x 匹配=0、返回按钮无箭头、功能完好（struct/canvas 切换正常）。
提交 a3f5420，push 成功。

## VOC 打标工具对齐参考页面（用户提供链接）
- 用户给参考页 https://ffswwwda.github.io/voc-report/tag-demo.html，要求主站 VOC 工具按该页「功能 + 输出结果」完全对齐。
- 新建独立 `voc-tagging.html`（Radium 风格，~95.7KB，单文件零依赖）：
  - 引擎：9 模块规则（用户画像/购买动机/购买目的/使用场景/使用阻碍/克服方式/用户忠诚度/产品改进建议/十三种需求），三级回退（精确三级→二级中类→一级板块）+ 否定保护（hasNegationBefore）。
  - 交互：粘贴文本 / CSV 上传（自动检测中文列名文本列）/ 模块开关 / 3 步流水线动画。
  - 输出：逐条证据+置信度卡片、标签云、模块覆盖条形（可排序）、用户旅程地图（6 阶段认知/评估/购买/使用/体验/复购 + 效果影响 + 情绪强度）、十三种需求映射、情绪罗盘（Plutchik 八情绪 SVG）、用户需求四象限、需求优先级气泡图、动机构成条形、小版块维度细分。
  - 参考页 emoji 图标按用户审美改为 Radium 渐变方块数字/纯文字；参考页 SheetJS Excel 导出改为**无依赖 CSV 导出**（BOM+CSV，避免 CDN 离线失败）。
- 主站 `openVocTool` 改为 iframe 嵌入 `voc-tagging.html`（新增 `.voc-frame` CSS）；旧 VOC_RULES / 语料分析工具保留未删。
- 验证：jsdom 独立页 4 条示例评价→33 精确标签 / 9/9 模块覆盖 / 全部可视化渲染 / 无运行时错误；主站 iframe 接入 + 回归（语料工具、旧规则）无影响。
- 提交 160cdf6 推送成功；线上 `voc-tagging.html` 与 `category-insight-hub.html` 均 HTTP 200。

## 结构化模式改左中右三栏布局（12:40 用户要求）
- 用户要求：结构化模式默认排版改为左中右三栏 —— 左边数据源、中间分析逻辑框架、右边分析结果/导出智能体提示词。
- 改动：
  - CSS：`.struct-panel` 从固定 392px 单栏改为 `display:flex` 三栏容器；新增 `.struct-col`（flex:1, overflow-y:auto, 独立滚动, border-right 分隔）+ `.col-header`（栏目标题，渐变底线）。
  - JS：`renderStructPanel` 内容拆分到 3 个 struct-col：左(数据源卡+关联洞察) / 中(方法链路+NL获取+智能建议+VOC引擎) / 右(基础分析结果+行动清单+导出)。
  - `applyMode`：struct 模式下 `sp.style.display='flex'`（原为 block）。
- 验证 jsdom：3 栏正确渲染 / 内容分布正确 / 模式切换正常。
- 提交 93eabf7 推送成功。

## 统一主题：德国看板 & VOC 工具跟随主站明暗（12:43 用户要求）
- 问题：主站有 body.dark 暗色主题，但两个 iframe 内嵌页（de-dashboard.html / voc-tagging.html）只有亮色，主站切暗后里面仍白，风格割裂。
- 方案（三页统一）：
  - 两个子页各加 `body.dark` 暗色变量覆盖（--bg/--panel/--glass/--border/--ink/--line 等翻转）+ 硬编码白底翻转（de: pill/src-tag/chip hover/scrollbar；voc: vt-select/textarea/mod-chip/csv-chip/cmt-card/file-drop/jm-emo-bar）+ body 暗色网格背景。
  - 子页初始化脚本（body 顶部）：主题 = URL参数 theme > localStorage cih-theme > light；并 `addEventListener('message')` 监听主站 `postMessage({type:'cih-theme',value})` 实时切换。
  - 主站：openDeDashboard / openVocTool 给 iframe src 追加 `?theme=`+当前主题；主题切换按钮 click 后 `document.querySelectorAll('iframe').forEach(postMessage)` 广播。
- 验证 jsdom：子页 ?theme=dark→body.dark=true、light→false；postMessage(dark)→body.dark=true；主站 openVocTool/openDeDashboard 生成 iframe src 含 ?theme=dark；切换按钮 dark→light 生效。
- 提交 f225cc8 推送成功。

## 德国看板暗色残留 + AI工具图标 + 新增预测工具（13:46 用户要求）
- **德国看板残留 bug**：上一轮主题统一后，暗色模式下 `.sidebar`(line60) 与 `.topbar`(line76) 背景仍是**硬编码白** `rgba(255,255,255,...)`，未用变量 → 暗色下左栏/顶栏仍白。另 `--glass-blur` 变量从未定义，所有 `backdrop-filter:var(--glass-blur)` 失效。
  - 修复：`:root` 补 `--glass-blur:blur(14px)`；新增 `body.dark .sidebar` / `body.dark .topbar` 暗色背景 `rgba(20,28,48,.6)` 覆盖。
- **AI 工具两个数字图标**：主站 `openAITools` 两卡片的 `<div class="ai-ico">01/02</div>` 改为 Radium 渐变圆角方块 + 内嵌 3D 几何白描边 SVG（VOC=等距立方体 / 语料=柱状 / 预测=上升曲线）。与用户确认的品牌 icon 风格一致。
- **新增预测工具**：`openAITools` 加第 3 张卡片「预测工具」，点击 `window.open('https://ffswwwda.github.io/mirofish-console/','_blank','noopener')`；新增 `openPredictTool()`；导航「AI 简化工具」计数 2→3 个；gal-h-sub 文案同步更新。
- 验证 jsdom：德国看板 body.dark=true + sidebar/topbar 暗色规则存在 + glass-blur 定义；主站 3 卡片 / 3 个 SVG 图标 / 无 01·02 文本残留 / openPredictTool 已定义 / 导航含「3 个」。
- 提交 146c2a5 推送成功。

## 用户语料分析工具：CSV 上传 + 情绪分析（14:00 用户要求）
- 用户要求：语料工具按 VOC 智能打标工具逻辑，增加 **CSV 上传**；做**用户语料本地化/情绪分析**；分析情绪、打标签、上传表格、**新建标签字段「情绪」**、导出 CSV。
- 改动（category-insight-hub.html 内 openCorpusTool / runCorpusAnalysis / exportCorpusCSV 等，+303 行 -27 行）：
  - 双模式：粘贴文本 / 上传 CSV（`corpusSwitch` 切换；CSV drop 用 Radium 渐变方块 + 内嵌 SVG 上传图标，非 unicode）。
  - CSV 引擎：`corpusParseCSV`（RFC4180 引号转义）/ `corpusColumnStats` / `corpusDetectCols`（自动检测中文列名 内容/评论/评价…）/ `corpusOnCsvSelected`（FileReader→列 chips 多选）/ `corpusToggleCol`。
  - **情绪分析引擎**：`EMOTION_DEFS`（Plutchik 八情绪 喜悦/信任/期待/惊讶/悲伤/厌恶/愤怒/恐惧 + 极性 + 配色）/ `EMOTION_RULES`（关键词→情绪）/ `classifyEmotion`（含 `emotionHasNeg` 否定保护 "不开心"≠喜悦）/ `EMOTION_ORDER`。
  - 分析输出：KPI（语料条数/正面%/负面%/命中维度）+ 正/中/负 pill 汇总 + 八情绪分布条形 + 维度标签 TOP5 + 逐行明细（维度 tp-tag + 情绪 emo-tag 双标签）。
  - 导出：`exportCorpusCSV`（无依赖 BOM+CSV）。CSV 模式表头=原始行号+原CSV各列+情绪+情绪极性+维度标签；粘贴模式=原评价+情绪+情绪极性+维度标签。对齐 VOC 工具的 csvCell 转义。
  - 新增 CSS：.csv-drop/.csv-sq/.csv-chip/.emo-tag/.emo-row/.emo-fill/.emo-summary/.emo-pill；复用 .rep-* / .tp-tag。
- 验证 jsdom 全 PASS：双 tab/textarea/drop/file 存在；classifyEmotion 正/负/中/否定保护正确；粘贴与 CSV 两模式均渲染情绪分布+emo-tag；导出表头 `原始行号,id,评论,渠道,情绪,情绪极性,维度标签` 且含原始评论（示例：静音…→喜悦/正面/功能、噪音）。无运行时错误。
- 提交 6349f22 推送成功。

## 产品打分工具 + 领新用户反馈专区（15:14 用户要求）
- **产品打分工具**（AI 工具页第 4 张卡片）：
  - 三维度 12 指标加权评分：市场价值 25%（需求热度/用户痛点/趋势性/市场规模）、商业价值 25%（利润空间/售价带/销量潜力/生命周期）、产品差异化 20%（差异化程度/竞争壁垒/幻想标签价值/卖点独特性）。
  - 每项 1-5 分滑块（Radium 渐变 thumb），拖动实时计算加权总分（0-5.00 渐变文字）+ SVG 雷达图（三轴 + 背景网格 5 环 + 数据多边形 + 标签圆点）。
  - 投资建议判定：≥3.8 绿色"建议推进"、2.6-3.8 黄色"谨慎观察"、<2.6 红色"暂缓投入"。
  - `openProductScore()` 函数 + `PS_DIMS` / `PS_SCORES` / `renderPSTotal()` / `buildPSRadar()`。
  - 新增 CSS：`.ps-dim/.ps-dim-head/.ps-item/.ps-slider/.ps-total-bar/.ps-radar-wrap/.ps-verdict(go/caution/stop)`。
- **领新用户反馈专区**（结构化模式左栏，数据源卡与关联洞察下方新增 section）：
  - 双 tab 切换：`lf-tab[data-lftab="portrait"]`(默认) / `"feedback"` → `switchLfTab()` 切换渲染。
  - **用户画像看板**：`LF_PORTRAITS` 示例数据（4 张身份卡：极客型早期采用者 / 场景定制需求者 / 审美优先体验派 / 性价比精算师）；每张卡含 Radium 渐变头像方块(字母) + 名称 + 标签 pills + 关键语引用 + 来源/超前性/创意度 meta。CSS `.uid-grid/.uid-card/.uid-avatar/.uid-name/.uid-tag/.uid-quote/.uid-meta`。
  - **用户反馈看板**：`FEEDBACK_RAW` 示例数据（5 条原始反馈：VOC小众×3 / 访谈特别×2）；每条含来源标签(voc橙/访谈紫) + 反馈正文 + 三指标 pill（超前性 lead / 创意度 creat / VOC命中率 hit-low）。CSS `.rf-list/.rf-row/.rf-src(.voc/.interview)/.rf-txt/.rf-metrics/.rf-metric(.lead/.creat/.hit-low)`。
  - `renderStructPanel` 末尾调用 `renderLfBody()`；左栏 HTML 结构在 `insHtml` 后追加 `<div class="struct-section lf-zone">...</div>`。
- AI 工具导航计数 3→4 个；gal-h-sub 文案同步更新为"四个工具"；新增 SVG 图标 ICON_SCORE（星形评分图标白描边）。
- 新增 DOM 容器 `viewProductScore`（id 在 viewCorpusTool 之后）。
- 验证 jsdom ALL PASS：产品打分 3维度/12滑块/雷达图SVG/全5=5.00/全1=1.00/结论stop类正确；领新反馈区 2 tab/4身份卡/5反馈行/超前·创意·命中指标全有；AI工具卡片4张/导航'4个'✓。
- 提交 dcaeaa6 推送成功。

### 产品打分工具重建 + 领新反馈区挂到 AI 工具页（c89c0b3）
- 产品打分工具 `openProductScore` 重建：放弃原滑块逻辑，改为 **自然语言 textarea + 文件上传(.txt/.csv/.docx/.xlsx)** → `decomposeInput` 拆解(类目/客单价/功能/外观/人群/场景) → `matchSources` 按信号匹配 8 数据源(加权匹配度) → `computeScoreReport` 按 3 大板块 12 维度自动评分。
- 每个维度输出：**分数 + 理由(为什么这样打) + 引用源 chip(点击 openSource 可追溯) + 置信度徽章(高/中/低/推测)**。置信度规则：源 ready 且 records≥100→高；ready→中；pending→低；无信号→推测。无数据时标记「推测」。
- 报告含 ①输入拆解 ②数据源匹配 ③评分结果(板块均分+结论+雷达图+加权总分)。维度打分细则用户说后续会详细定义，当前为合理启发式框架。
- docx/xlsx 用 `DecompressionStream('deflate-raw')` 提取 XML 文本(best-effort, 失败回退提示导出 txt/csv)。
- 领新用户反馈专区：用户反馈「看不到、应放在 AI 简化工具下方」→ 在 `openAITools` 落地页 4 卡片下方新增 `.lf-zone.gal-lf`，复用 renderLfPortrait/renderLfFeedback。
- 重构 `renderLfBody(zone)/switchLfTab(tab,el)` 为 **zone-local**（tab 状态按 zone 独立，不再用全局 lfCurrentTab），使结构化左栏与 AI 工具页两处领新区互不干扰。
- jsdom 全验证：拆解(飞机杯/60/震动/外观)正确、匹配Top3正确、置信度高8低4、弱输入推测4、报告面板3行12、AI页4卡+领新区、结构化左栏独立portrait。

### 领新用户反馈专区改为与 AI 简化工具「平级」（最新，16:10 用户纠正）
- 用户澄清：之前 c89c0b3 把领新区嵌在 AI 工具页「卡片下方」是**里面**，用户要的是和「AI 简化工具」**同一层级**（独立导航入口）。
- 改动：
  - HTML 新增独立 view `<div class="view" id="viewLfZone"></div>`（与 viewAITools 平级）。
  - `renderNavShared` 在「AI 简化工具」(nav-ai) 后新增独立 nav-item「领新用户反馈专区」(data-nav="nav-lf" → `openLfZone()`)。
  - `openAITools` 删除内嵌的 `.lf-zone.gal-lf` 区块与 aiZone 渲染，文案去除"下方为领新用户反馈专区"——现在只承载 4 个工具卡片。
  - 新增 `openLfZone()`：setActiveNav('nav-lf') + setView('LfZone') + 渲染 gal-head + `.lf-zone.lf-page`（独立卡片，含双 tab + lf-body，调用 renderLfBody 渲染画像/反馈）。
  - CSS：新增 `.lf-page`（同 .gal-lf 卡片感：border/glass/padding），独立页面铺满对齐 gal-head。
  - tab 状态仍 zone-local（switchLfTab 用 closest('.lf-zone')），现在三处领新区（结构化左栏 / 独立页面）互不干扰。
- jsdom 验证全 PASS：导航 4 项含「领新用户反馈专区」、AI 页无 lf-zone 且 4 卡片、独立页激活+zone 存在+2 tab+画像 card、切 feedback 渲染 rf-list、nav-lf 高亮。
- 已 commit + push 成功（working tree clean）。
