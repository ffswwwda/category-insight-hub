# 2026-07-15

## 构建「类目用户研究数据库」单文件看板
- 交付物：`/Users/fsw/WorkBuddy/2026-07-15-08-51-27/category-insight-hub.html`（单文件 HTML，零依赖、可离线、双击即开）
- 架构参考用户给的 content-analytics-dashboard（ffswwwda.github.io）：数据驱动 SPA 壳（侧栏导航+全局组合筛选+详情抽屉+导出），数据层与渲染解耦。
- 信息架构：数据源 / 分析框架 / 打标签方法 / 分析结果 / 品类洞察 + 7 个目的驱动工作区（新品打造/新品规划/产品类目规划/营销建议/市场细分/产品优化/用户问题研究）+ AI工具台。
- 「不接大模型的 AI 工具」实现方式：把 voc-tagger 等规则引擎移植成网页 JS（含否定保护，本地运行）+ 内嵌完整 skill 方法论手册。已实装 VOC/社媒/语料 三个规则引擎，node 实测否定保护正确。
- 数据层为单一 `DATA` 对象，用户后续替换该对象即可接入真实抓取数据；当前为占位类目「智能收纳」示例数据。
- 验证：node --check 语法通过；规则引擎功能测试通过（否定保护、示例标签正确）。
- 待用户确认：① 指定真实类目+市场 ② 提供真实数据源（JSON/CSV/文档）以替换 DATA ③ 是否需部署成在线链接。

## 重构为「双工作区」架构（第二轮）
- 用户提出二分法：A 用户洞察·创意（从用户出发向外推：细分/机会/优化或开发或品牌）；B 运营目的·解题（从具体问题反向调用用户端口信息与方法）。已重构进 `category-insight-hub.html`。
- 每个 purpose 现绑定 `sources[]/dims[]/methods[]/tool`，进入即自动只切出相关数据源与洞察；数据源卡片反向显示「被哪些目的调用」（双向打通）。workspaces.A.purposes=[xifeng,opportunity,persona]；B=[xinpin_dazao,xinpin_guihua,leimu_guihua,yingxiao,youhua,wenti]。
- 新增「市场细分」按标签切分：源数据带标签（购买目的/用户画像/使用场景/痛点/需求 5 维，30 条 segmentRecords），选维度→看分布条形→点开细分看用户语料+关联洞察。
- 新增「导出到智能体」提示词构建器（顶栏按钮 + 各目的内按钮）：自动提取当前视图上下文（工作区/目的/绑定源结论/相关洞察/当前细分）+ 用户输入的问题 → 生成可复制/导出的提示词（角色+上下文+我的问题+要求）。这就是用户说的「人和 AI 的结合」。
- 验证：修复 workspaces 漏写 purposes 数组的 bug；node 实测 规则引擎否定保护、按标签切分计数合计=30、上下文提取、提示词构建、反向绑定 全部通过。

## 无限画布模式（第三轮 · 关系连线版）
- 用户确认画布完成度为「关系连线版」。已在每个目的工作区顶部加「结构化 / 无限画布」模式切换：结构化=系统按目的切好结论（可答）；无限画布=自由探索（可发散）。
- 画布能力（关系连线版）：滚轮缩放、空白处拖动平移、Shift+拖拽框选、从「素材」抽屉拖入数据源/洞察/工具/批注卡、卡片自由拖动、右侧圆点拉线连成关系/推导图、双击文字编辑批注、选中后 Delete 删除节点/连线、状态按目的自动存 localStorage（键 cih_canvas_<purposeId>），刷新/重进不丢。
- 结构化→画布的「一键带入」：数据源条「把这些源加入画布→」、每个洞察卡「＋画布」按钮，均调用 addSourcesToCanvas/addInsightToCanvas 后切到画布模式。
- 画布同样尊重数据源边界（素材抽屉只列本目的绑定源），但允许跨源自由组合与连线（自由度最高的地方）。
- 两种模式最终都汇入「导出到智能体」：gatherContext 在 state.mode==="canvas" 时把节点/连线纳入上下文提示词。
- 验证：node --check 语法通过；harness 实测 seedCanvas(4源+4洞察)、addSourcesToCanvas 持久化、addInsightToCanvas 去重、gatherContext 画布分支（含"无限画布""连线关系"）、规则引擎否定保护 全部通过。
- 整体形态已具备部署 GitHub Pages 条件（单文件内联数据，规避 fetch 跨域）。

## 核心角色重定义（第四轮 · 设计哲学层）
- 用户对「用户」的定义重定义：**操作本工具的人 = 创作者（creator），而非使用者（consumer）**。
  - 注意区分：本工具研究的是「终端用户/消费者」（亚马逊买家等），但**工具的 operator = 我（创作者）**。不要在 UI/提示词里把 operator 叫成"使用者/读者"。
  - 范式：旧=报告→使用者（被动消费）；新=素材→无限画布→创作者（主动创造）。
- 由此推论：**无限画布是「重要的工作区」/核心工作台**，而非次要模式。数据源/框架/打标/AI规则引擎都只是喂给创作者的「输入素材」；洞察在画布上由创作者组合、连线、推导而生。结构化视图是辅助入口，画布才是价值主战场。
- 对后续实现的约束：①画布应更突出（可考虑作为默认或主入口）；②提示词构建器/角色设定应把 operator 定位为「创作者」；③所有板块定位为「创作者可用的素材库」而非「给使用者看的报告」。

## 画布设为默认主入口（第五轮 · 落地）
- 用户确认：进任一目的**直接进无限画布**（creator-first），结构化结论收进左侧可折叠「📋 结论」抽屉，按需展开。
- 实现：①`setSection`/`switchWorkspace` 进入 purpose 时 `state.mode="canvas"`；②`renderPurpose` 删掉原「📋结构化/🎨画布」切换条，只渲染 `renderCanvas`；③新增 `buildConclusions(k,host)`（数据源条+方法、相关洞察带＋画布、行动清单、引导问题、推荐工具、导出智能体、xifeng 含市场细分），作为左侧抽屉 `#cv-conclusions`（默认 open，344px，左贴边，避免与右侧「素材」抽屉冲突）；④工具栏加「📋 结论」按钮切换；⑤提示词角色改为「我是类目用户研究的创作者，你是我的思考伙伴」（buildPrompt）。
- 验证：node --check 通过；harness 实测 进入新品打造/xifeng 均 OK、paintSegment 不崩、规则引擎否定保护未被破坏、提示词含"创作者/思考伙伴"。
- 当前形态：画布=核心工作台，左侧结论抽屉=系统答案（可答），右侧素材抽屉=拖卡入画，两者+导出智能体闭环完整。仍待用户给真实类目/8个数据源以替换 DATA。

## 画布布局「视频软件式」重构 + 丝滑贝塞尔连线（第六轮 · 当前）
- **用户反馈**：①连线不丝滑（直线/简单S弯不够）②画布应像视频生成软件（Runway/Pika）：上方结构化面板轨道（数据源→分析框架→输出结果），下方才是无限自由画布。
- **连线升级**：`smoothPath(a,b)` — 从源卡片**右边缘出口** → 到目标卡片**左边缘入口**，用三次贝塞尔曲线 `C`，控制点按水平间距的45%外扩形成自然S弯。垂直偏移时曲线自动适应。`startConnect`预览线也同步升级。加 `stroke-linecap:round` 让端点圆滑。
- **布局重构**：
  - `canvas-root` 改为 `display:flex;flex-direction:column`
  - 新增 `.canvas-panels` 横向三栏（`.cpanel`）：📥 数据源 / 🧭 分析框架 / 📊 输出结果
    - 每个面板可折叠（点击头部 toggle `collapsed` class），默认展开，max-height 280px
    - 数据源面板：每条可拖拽入画布、带「＋画布」快捷按钮
    - 分析框架面板：聚焦维度+分析方法链路步骤
    - 输出结果面板：洞察卡片（可点开详情/＋画布）+ 行动清单+引导问题+推荐工具+导出智能体
  - 下方 `.canvas-workspace`（`flex:1`）包含无限画布内容区+SVG连线层+提示条
  - 删除旧的左侧「📋 结论」抽屉和模式切换条；三面板直接替代
- **删除** `buildConclusions()` 函数，替换为 `buildThreePanels(k)` 填充三个面板
- 验证：node --check 语法通过；harness 实测 smoothPath 数学正确（右边缘M→左边缘结束、控制点在中间）、三面板渲染不崩溃、规则引擎否定保护未被破坏。

## 交互式三面板 + 自然语言筛选 + 模式恢复（第七轮 · 当前）
- **用户反馈**：①不知道怎么切回默认模式（上一版删了切换条）；②数据源卡片应可展开填自然语言筛选（如"50美元左右的差评排序"）；③分析框架应按目的推荐标签；④输出基础结论+完整提示词给AI。
- **修复默认模式**：`setWorkspace`/`setSection` 进入 purpose 时 `state.mode="structured"`（不再强制 canvas）；`renderPurpose` 加回「📋 结构化（默认）/ 🎨 无限画布」切换条。
- **数据源自然语言筛选**：新增 `DATA.sourceRecords` 样本（s1/s2/s8 各 6/4/4 条，含 price/star/text/tag 字段）；`filterSourceRecords(sid,query)` 支持解析：价格（单点+容差"左右"）、价格区间（"30到40美元"）、星级（差评≤2/好评≥4/中评=3/X星/X星以上/X星以下）、排序方向（从高/从低/降序/升序/最便宜）、关键词匹配（去中文虚词+去数字+去功能词后的剩余词），返回匹配记录+排序。停用词表含"以上/以下/便宜/贵/最"等。UI：每个数据源卡片可展开→输入框→实时出匹配条数+记录列表。
- **分析框架推荐标签**：新增 `FW_LIB`（22 个框架方法标签，如"痛点→卖点""需求映射""JTBD分析"等，每个标明适用目的）；`recommendFrameworks(k)` 按目的方法链路推荐；UI 渲染可点选 tag，选中项存 `state.fwSelected[k]`（Set），样式高亮。
- **基础结论+提示词**：新增 `basicConclusion(k,activeFilters,selectedFw)` 生成基于当前筛选+框架的基础结论文本；输出面板新增「📋 基础结论」区块（可展开）。`gatherContext` 新增：数据源定向筛选（每个激活源的查询+命中数+前3条记录摘要）、选用分析框架（选中标签列表）、分析框架逻辑（标签对应的说明）。导出到智能体的提示词现在完整包含：目的+绑定数据源+定向筛选结果+选用框架+框架逻辑+基础结论+用户问题。
- **验证**：node --check 语法通过；9/9 功能测试全部通过（含8种自然语言查询、框架推荐、默认模式校验、结构化/画布渲染、上下文含筛选+框架、规则引擎否定保护、市场细分不崩）。

## 新增：画布节点「点击弹出筛选看板」（用户核心诉求）
- 需求：把数据源卡片拖到无限画布后，点击它 → 弹出卡片让用户输入筛选（如"50美元 1-3星差评"）→ 展示看板结果（多少款商品/差评条数/平均星/Top分类标签/市场分布/大概说了什么）+ 可折叠的筛选后原始数据。
- 实现（category-insight-hub.html）：
  - `filterSourceRecordsAll(sid,q)` 全量版（不截断）供看板聚合；`filterSourceRecords` 保留为其 slice(0,15) 截断版给行内小结果。
  - `computeBoard(sid,q)` 聚合：skuCount(按价格档近似去重)/bad-mid-good/avgStar/tags(降序)/markets/价格区间。`boardSaysText` 生成"说了什么"摘要(Top标签+代表差评/好评句)。
  - `openNodeDashboard(node)`+`renderNodeBoard()`+模态 #nb-modal（KPI 卡×4 + 标签条形图 + 市场 pill + 说了什么 + <details> 原始数据折叠 + 快捷 chips）。
  - 交互：bindNode 的 startDrag 加 dragged 判定，source 节点"未拖动=点击"打开看板；节点内另加"📊 看板"按钮双保险。看板筛选写入 state.filters[pkey|sid]，与结构化/画布/导出提示词共用。
- 关键 bug 修复：查询"1-3星差评"里的"1-3"被价格区间正则误判为 $1-3。解决：先识别星级区间 `([1-5])(-|到|至|~)([1-5])星`，价格区间正则加 `(?!\s*星)` 负向断言并在 starRange 命中时跳过。
- 验证：node new Function 语法通过；DOM 桩测 6+ 断言全过（1-3星差评命中5条/5款商品/4差评/2.0星/Top标签正确/摘要生成/全量看板）。

## 三层架构改造（上午，三层全做）
用户把看板拆成三个层级，全部落进 category-insight-hub.html：
- **第一层·数据源看板二级页**：renderSources 卡片加「📊 打开数据源看板」按钮 → state.srcView 进入 renderSourceDetail(b,sid)。二级页含：面包屑返回、数据源综合报告、自然语言筛选+看板报告(复用 computeBoard/boardSaysText：KPI4卡/标签条/市场/说了什么/<details>原始数据折叠)、导出(.txt/复制/🤖带源生成深度提示词→openSourceDeepPrompt 复用 pmodal)。侧栏「数据源」仍为入口层，不逐源平铺。
- **第二层·目的工作区建议+基础分析**：renderStructured 顶部 insertBefore 插入 renderPurposeOverview(建议面板+基础四件)。recommendForPurpose(key) 基于 DATA 推断：交叉源(市场补缺度打分 top3 标注补XX市场) + 推荐分析框架(按目的label关键词匹配 KANO/Persona-STP/CJM/4P/痛点根因/增长矩阵)。基础四件：需求四象限(drawQuadrantSmall复用 matrix)、理论框架视角(relatedInsights)、细分市场(basicSegmentCard/paintOverviewSegment 按 SEG_FIELD 切分)、购买旅程(basicJourneyCard 用 segmentRecords 场景串联5步)。
- **第三层·全量上下文提示词**：升级 gatherContext 的 purpose 分支——带 目的+绑定数据源清单(每源结论)+分析框架链路+基础结论(重点机会象限)+定向筛选命中+建议补充源。buildPrompt 目的模式角色改为"把智能体当第一次看数据"，要求先复述理解再深度多轮推导，区分已支持/需验证/外部补充。非目的模式保持原简洁版。
- 验证：node 整文件语法通过；DOM 桩 15 断言全过(recommend 框架/交叉源/画像框架/computeBoard/gatherContext 四大块/buildPrompt 目的vs非目的差异)。
- 注意：recommendForPurpose 用"市场补缺度"粗规则，真实精准关联需用户补 purposes[k].relatedSources 之类字段后再细化。

## 运营目的·解题 向导（11:08 需求，已落地）
用户明确：运营目的·解题方向应是「我输入目的 → 关键词拆解目的 → 生成分析框架+对应数据源字段 → 再生成提示词」，而不是点固定目的列表。
改造（category-insight-hub.html）：
- 新增 `DATA.purposeDict`（goal/market/cat/dim/atype 五类意图词典，含短语跨度补"差评/研究/新品"等短词）+ `DATA.frameworkFields`（12个框架→所需数据源字段映射，来自 segmentDims/voc/social/corpus 维度）。
- `decomposePurpose(text)`：按词典拆结构化标签；意图反推补全(痛点根因→goal优化, 旅程/细分→洞察)；映射到预设目的评分(goal+6强信号, dim硬匹配+3, atype→methods正则+3, atype文本+2, cat+1)；兜底用 recommendForPurpose。
- `renderPurposeWizard(b)`：四段式 UI（① 自由输入框+3个示例chip ② 关键词拆解 ③ 分析框架+字段汇总 ④ 生成提示词按钮）。`openWizardPrompt(text,dec0)` 复用 pmodal，提示词含 原文+拆解+可衔接预设目的+框架+字段，角色"把智能体当第一次见数据"，要求先复述理解再推导。
- 导航：工作区 B 侧栏顶部加「🧭 目的解题向导」入口，且 setWorkspace("B") 默认进向导（区别于工作区 A 进第一个目的）。renderBoard 加 purpose-wizard 分派。
- 验证：node 整文件语法通过；DOM 桩 3 样例全断言过（降低美国收纳差评→youhua/痛点根因/尺寸承重；日本小户型新品→xinpin_guihua/KANO；旅程痛点→wenti/CJM）。修了一个坑：goal 短语跨度导致 includes 失败(用反推补全+短词解决)，及 goalPurpose 洞察组漏 wenti(实际在1708行另一处字符串，已补)。

## 纠正：目的解题方向（11:23 用户纠正）
之前把"运营目的·解题"做成独立 wizard 页(脱离目的树)，用户纠正：大目的(如新品打造)是已知点进去的，下面仍是「数据源/分析框架/输出结果」三栏；要在固定目的内部加"我输入更具体目的→拆解→给出(目的是什么/建议框架/建议数据源(可自然语言筛选)/输出结果提示词)"。
改造（category-insight-hub.html）：
- 删掉独立 purpose-wizard 入口与默认进 wizard；工作区 B 改回默认进第一个固定目的。
- 新增 `renderDecomposer(key)` 嵌入 `renderPurposeOverview` 顶部：文本输入框+实时拆解标签(业务目标/市场/品类/痛点维度/分析类型)+建议框架+建议数据源(每个已绑定源带自然语言筛选框,点"看板"→openNodeDashboard并预填筛选)+建议补充交叉源(复用recommendForPurpose)+"🤖输出结果:生成提示词"(openWizardPrompt(text,dec0,key))。
- `openWizardPrompt` 加 key 参数：提示词现含「我当前所在的大目的(工作区)+已绑定源」上下文+具体目的拆解+建议框架+字段+建议交叉源+复述理解要求。
- 删除死代码 renderPurposeWizard。修了一个坑：之前 Edit 误删 decomposePurpose 函数头导致语法错，已补回函数头。
- 验证：node 语法通过；DOM 桩 10 断言全过(renderDecomposer 四段结构/openWizardPrompt 含大目的/框架/交叉源/复述/setWorkspace B 默认固定目的)。

## 媒体筛选 bug 修复（续 11:23 后，收尾）
- 自然语言筛选 `filterSourceRecords` 的媒体过滤存在两个坑，已全部修复：
  1. **大小写**：`wantVideo`/`wantAplus`/`wantFBM` 正则原来区分大小写，用户输入"有A+页面"/"Video"会匹配失败。已统一加 `i` 标志。
  2. **"或"语义**：新增 `wantMediaOr`（查询含"或/或者/or"且同时提了视频+A+时）→ 记录满足视频或A+任一即通过；否则保持分别要求（"和"则两者都要）。
- 逻辑已落地 category-insight-hub.html（约 1754-1791 行过滤段）。
- 验证：node 整文件语法通过；独立 harness 7/7 用例全过（有视频/有A+页面/有a+页面/a+/有视频和A+页面/有视频或A+页面/有Video）。
- 当前文件已完整、可离线双击打开。下一步：接入真实数据源 + 设计升级（见下方）。

## 接入真实数据源 + 设计升级（14:38）
- **真实数据接入**：`de_records.json`（248 条德国站倒模类目商品）转为 `de_records.js`（const DE_RECORDS_FULL = [...]），通过 `<script src="de_records.js">` 在主 script 前加载。替换 `DATA.sourceRecords.s_de_real` 的 18 条占位为完整 248 条。清理了 186KB 内联残留碎片。
  - 数据源卡片标注"真实数据"黄色 pill；侧栏显示"248条 真实"；脚注说明真实/占位混合状态。
  - `_hay` 懒计算兼容真实记录字段（title/brand/cat/catPath/sellerLoc），筛选全线可用。
- **fix: const→var**：de_records.js 原用 `const DE_RECORDS_FULL`，但浏览器中 `const` 跨 `<script>` 标签不可见，导致页面空白。已改为 `var DE_RECORDS_FULL`。
- **fix: CSS 回退**：之前的多余设计升级全部撤销，CSS 恢复为原始版本（去噪点/原生渐变/原始配色/原始交互态/原始字体），只保留数据集成改动（`de_records.js` script 引入 + DATA 引用 + 真实数据 badge + 侧栏标记）。
- 验证：node 整文件语法通过。双击 category-insight-hub.html 打开 → 侧栏「数据源」→ 打开"亚马逊德国·倒模类目销售数据"（带"真实数据"标记）→ 体验 248 条真实数据的筛选/看板/画布功能。所有设计保持原样。

## 四项需求落地 + 推送 GitHub Pages（收尾）
用户四需求全部落地并 jsdom 验证通过后，git 提交 `8c50bee` 并 push 成功（此前 favicon「研」字 SVG 已提交 `a71ed60`，本轮一并上线）：
1. **运营目的扩充**：ops 工作区从 4 个目的扩至 8 个（新增 category-plan 类目规划 / price-strategy 价格策略 / campaign 活动运营 / competitor 竞品对标），补充 PURPOSE_FW 框架映射 + seedInsights 各 3 条。
2. **图标精简**：移除全部 emoji/unicode 图标（源数据区、目的网格、框架画廊、AI工具卡、功能按钮、导航分区头、搜索框）。改用 `.mini-idx`（22px 发丝边框序号）与 `.gal-idx`（32px 序号）+ 纯文字，贴合 Radium 极简玻璃风。
3. **德国看板风格统一**：de-dashboard.html 由扁平蓝白风改为 Radium 玻璃霓虹风（仅改 CSS）：--rad-grad 渐变、玻璃面板 rgba(255,255,255,.72)+backdrop-filter、光斑网格背景、霓虹细条 .stat::before/.panel::before、accent 改 #00C3F3、brand-mark 渐变。数据/JS 未动。
4. **画布默认隐藏**：renderPurposeWorkspace 改为 applyMode('struct', p)。默认进目的=结构化界面（canvas-area display:none），仅用户点击「无限画布」模式才 renderCanvasArea 并显示。enterPurpose 增加跨工作区自动定位（遍历 DATA.workspaces 找所属 ws）。
- 验证（jsdom）：hub 入 competitor(ops)→canvas=none/struct=block、切 canvas→canvas=flex/struct=none、ops 8 目的、无 &#x 图标实体、VOC 规则正常；de 248 条渲染正常、body 背景渐变生效、无 JS 报错。
- 线上验证：穿透缓存后 category-insight-hub.html 含 applyMode/mini-idx/category-plan/price-strategy/competitor；de-dashboard.html 含 rad-grad/glass-blur/backdrop-filter（均命中）→ 两文件均已上线 GitHub Pages。
- 待用户决定（此前悬停）：①8 个目的命名是否换回最早"市场细分/找到机会/优化产品…"那套；②是否接入真实 8 数据源替换样本生成器；③是否改默认主题为 dark。
